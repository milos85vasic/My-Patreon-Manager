// Package filter implements repository selection and .repoignore filtering,
// applying inclusion and exclusion rules to determine which repositories
// are processed during sync operations.
package filter
