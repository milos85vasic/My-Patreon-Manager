# Module 05 Exercise: Publishing

1. Run `./patreon-manager publish --dry-run` — review proposed posts
2. Publish one post: `./patreon-manager publish --tier=public`
3. Re-run the same command — verify no duplicate is created
