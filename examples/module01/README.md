# Module 01 Exercise: First Run

1. Clone the main repo
2. Run `go build ./...`
3. Copy `.env.example` to `.env` and set `GITHUB_TOKEN`
4. Run `./patreon-manager validate`
5. Run `./patreon-manager sync --dry-run`
